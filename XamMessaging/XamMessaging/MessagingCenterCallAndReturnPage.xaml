<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xamMessaging="clr-namespace:XamMessaging;assembly=XamMessaging"
             x:Class="XamMessaging.MessagingCenterCallAndReturnPage"
             Title="MessagingCenter">
    <ContentPage.BindingContext>
        <xamMessaging:MessagingCenterCallAndReturnViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>

            <Button Command="{Binding CallViewCommand}" Text="Call View from View Model"/>

            <ScrollView
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                BackgroundColor="Yellow">
                <StackLayout x:Name="ReportStackLayout"
                    Orientation="Vertical"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"
                    BackgroundColor="Aquamarine"/>
            </ScrollView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>